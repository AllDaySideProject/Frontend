import React from 'react';

export const AddButton = () => {
  const buttonStyle = {
    width: '24px',
    height: '24px',
    borderRadius: '50%',
  };

  return <button style={buttonStyle}>+</button>;
};
